# Brand files

`fitness-hub-logo.svg` is the only editable logo source.

```sh
npm run brand:sync
npm run brand:check
```

The pipeline generates every web icon, Android icon, splash asset, and notification mark. Never
redraw the logo or edit generated copies.
